name = "oxigeno-chat"
main = "worker.js"
compatibility_date = "2024-11-01"

[vars]
ALLOWED_ORIGIN = "https://yonatanzlit.github.io"
FROM_EMAIL = "no-reply@oxigenoweb.com"
MODEL = "claude-haiku-4-5-20251001"

# Secrets (do NOT put here — set with `wrangler secret put`):
#   ANTHROPIC_API_KEY
#   SENDGRID_API_KEY
